# Vercel KV (Upstash Redis) Environment Variables
# Получите эти переменные после подключения Upstash Redis через Vercel Marketplace
# https://vercel.com/integrations/upstash

# REST API URL для подключения к Upstash Redis
KV_REST_API_URL=https://your-database-name.upstash.io

# REST API Token для аутентификации (read/write)
KV_REST_API_TOKEN=your_token_here

# REST API Read-Only Token (опционально)
KV_REST_API_READ_ONLY_TOKEN=your_readonly_token_here

# 📝 Инструкция:
# 1. Скопируйте этот файл: cp .env.local.example .env.local
# 2. Подключите Upstash Redis в Vercel Marketplace
# 3. Скопируйте переменные из Vercel Dashboard → Settings → Environment Variables
# 4. Вставьте реальные значения в .env.local
# 5. Файл .env.local автоматически игнорируется git'ом

